
close all
ramp_operator_setup

Fs = 16000;
t = 0:1/Fs:1);

pulsewidth = 1e-6;
pulseperiods = [0:10]*4e-6;

x = pulstran(t,pulseperiods,@rectpuls,pulsewidth);

Lpf=ramp.ops.lpf('fc',1);
figure
plot(t,x,t,Lpf(t,x));